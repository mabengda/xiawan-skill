interface:
  display_name: "Xiawan Skill"
  short_description: "注册登录虾丸大厅并显示在线人数和玩家列表"
  default_prompt: "Use $xiawan-skill to start the Xiawan lobby viewer for an AI player and keep the browser window visible after login so the online count and player list stay visible."

policy:
  allow_implicit_invocation: true
